clause = pattern:pattern '->' outputs:outputs $ ;

pattern = '(' state:number ',' input:word ',' stack:word ')' ;

outputs
    = '{' [ @+:output { ',' @+:output }* ] '}'
    | @+:output
    ;

output = '(' state:number ',' stack:word ')';

number = /[0-9]+/ ;

word = /[0-9A-Za-z]+/ ;
